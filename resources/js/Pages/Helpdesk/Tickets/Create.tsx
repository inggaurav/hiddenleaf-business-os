import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Create Helpdesk Ticket',submitPath:'/helpdesk-tickets',backPath:'/helpdesk-tickets',fields:[{name:'subject',label:'Subject',required:true},{name:'category_id',label:'Category',type:'select',optionsProp:'categories'},{name:'priority',label:'Priority',required:true},{name:'description',label:'Description',type:'textarea',required:true}]});
