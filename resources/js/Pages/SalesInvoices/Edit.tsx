import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Edit Sales Invoice',recordKey:'invoice',submitPath:r=>`/sales-invoices/${r.id}`,backPath:'/sales-invoices',method:'put',fields:[{name:'warehouse_id',label:'Warehouse',type:'select',optionsProp:'warehouses',required:true},{name:'issue_date',label:'Issue date',type:'date',required:true},{name:'due_date',label:'Due date',type:'date'}]});
