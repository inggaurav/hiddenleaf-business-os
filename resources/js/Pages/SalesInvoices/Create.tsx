import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Create Sales Invoice',submitPath:'/sales-invoices',backPath:'/sales-invoices',lineItems:true,fields:[{name:'customer_id',label:'Customer ID',type:'number'},{name:'warehouse_id',label:'Warehouse',type:'select',optionsProp:'warehouses',required:true},{name:'issue_date',label:'Issue date',type:'date',required:true},{name:'due_date',label:'Due date',type:'date'}]});
