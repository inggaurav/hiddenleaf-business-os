import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Create Transfer',submitPath:'/transfers',backPath:'/transfers',fields:[{name:'from_warehouse',label:'From warehouse',type:'select',optionsProp:'warehouses',required:true},{name:'to_warehouse',label:'To warehouse',type:'select',optionsProp:'warehouses',required:true},{name:'product_id',label:'Product ID',type:'number'},{name:'quantity',label:'Quantity',type:'number',required:true},{name:'date',label:'Transfer date',type:'date',required:true}]});
