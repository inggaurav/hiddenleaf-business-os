import DataPanel from '@/Components/DataPanel';

export default function Manage() {
  return <DataPanel title="Landing Pages" description="Configure public SaaS content, sections, custom pages, SEO, ordering, and publishing." />;
}
