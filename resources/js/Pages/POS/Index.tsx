import DataPanel from '@/Components/DataPanel';
export default function POSIndex() { return <DataPanel title="Point of Sale" description="Registers, cash sessions, product lookup, checkout, receipts, payments, and returns." />; }
