import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Edit Plan',recordKey:'plan',submitPath:r=>`/plans/${r.id}`,backPath:'/plans',method:'put',fields:[{name:'name',label:'Name',required:true},{name:'description',label:'Description',type:'textarea'},{name:'package_price_monthly',label:'Monthly price',type:'number',required:true},{name:'package_price_yearly',label:'Yearly price',type:'number',required:true},{name:'number_of_users',label:'Users',type:'number',required:true},{name:'workspace_limit',label:'Workspaces',type:'number'}]});
