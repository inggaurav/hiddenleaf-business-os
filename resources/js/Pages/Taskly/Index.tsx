import DataPanel from '@/Components/DataPanel';

export default function TasklyIndex() {
  return <DataPanel title="Projects & Tasks" description="Projects, kanban stages, milestones, tasks, timesheets, costs, comments, and issues." />;
}
