import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Edit Purchase Invoice',recordKey:'invoice',submitPath:r=>`/purchase-invoices/${r.id}`,backPath:'/purchase-invoices',method:'put',fields:[{name:'warehouse_id',label:'Warehouse',type:'select',optionsProp:'warehouses',required:true},{name:'purchase_date',label:'Purchase date',type:'date',required:true},{name:'due_date',label:'Due date',type:'date'}]});
