import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Create Purchase Invoice',submitPath:'/purchase-invoices',backPath:'/purchase-invoices',lineItems:true,fields:[{name:'vendor_id',label:'Vendor ID',type:'number'},{name:'warehouse_id',label:'Warehouse',type:'select',optionsProp:'warehouses',required:true},{name:'purchase_date',label:'Purchase date',type:'date',required:true},{name:'due_date',label:'Due date',type:'date'}]});
