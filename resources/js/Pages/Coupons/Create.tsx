import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Create Coupon',submitPath:'/coupons',backPath:'/coupons',fields:[{name:'name',label:'Name',required:true},{name:'code',label:'Code',required:true},{name:'type',label:'Type',required:true},{name:'discount',label:'Discount',type:'number',required:true},{name:'limit',label:'Usage limit',type:'number'},{name:'expiry_date',label:'Expiry date',type:'date'}]});
