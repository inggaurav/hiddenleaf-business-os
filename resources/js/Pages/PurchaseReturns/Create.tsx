import { createFormPage } from '@/Components/ResourcePage'; export default createFormPage({title:'Create Purchase Return',submitPath:'/purchase-returns',backPath:'/purchase-returns',lineItems:true,fields:[{name:'vendor_id',label:'Vendor ID',type:'number'},{name:'purchase_invoice_id',label:'Purchase invoice ID',type:'number'},{name:'date',label:'Return date',type:'date',required:true}]});
