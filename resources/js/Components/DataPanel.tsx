import React from 'react'; import { Head, usePage } from '@inertiajs/react'; import AppShell from '@/Layouts/AppShell';
export default function DataPanel({title,description}:{title:string;description:string}){const props=usePage<any>().props; const data=Object.entries(props).filter(([key])=>!['auth','tenant','flash','errors','ziggy'].includes(key)); return <AppShell><Head title={title}/><div className="space-y-6"><div><h1 className="text-2xl font-bold text-slate-900">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p></div><div className="grid gap-4 lg:grid-cols-2">{data.map(([key,value])=><section key={key} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-3 font-semibold capitalize text-slate-900">{key.replace(/_/g,' ')}</h2>{Array.isArray(value)?<div className="space-y-2">{value.length?value.map((item:any,index)=><div key={item?.id??index} className="rounded-lg bg-slate-50 p-3 text-sm text-slate-700">{typeof item==='object'?item.name??item.title??item.subject??JSON.stringify(item):String(item)}</div>):<p className="text-sm text-slate-500">No records yet.</p>}</div>:<pre className="overflow-auto whitespace-pre-wrap text-xs text-slate-600">{JSON.stringify(value,null,2)}</pre>}</section>)}</div></div></AppShell>}
